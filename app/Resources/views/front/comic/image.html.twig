<img
  id="incredible_{{ comic.getId().getComicId() }}"
  class="img-thumbnail"
  src="{{ comic.getThumbnail().getPath()|https }}/portrait_incredible.{{ comic.getThumbnail().getExtension() }}"
  alt="{{ comic.getTitle()|title }}"
  height="324"
  width="216"
/>
