<img
  id="incredible_{{ event.getId().getEventId() }}"
  class="img-thumbnail"
  src="{{ event.getThumbnail().getPath() }}/portrait_incredible.{{ event.getThumbnail().getExtension() }}"
  alt="{{ event.getTitle()|title }}"
  height="324"
  width="216"
/>
