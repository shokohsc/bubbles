<img
  id="incredible_{{ comic.getId().getSerieId() }}"
  class="img-thumbnail"
  src="{{ comic.getThumbnail().getPath() }}/portrait_incredible.{{ comic.getThumbnail().getExtension() }}"
  alt="{{ comic.getTitle()|title }}"
  height="324"
  width="216"
/>
