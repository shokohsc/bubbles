{% for comic in collection %}
  <div class="comic-item-list">

      <a href="{{ path('serie', {'id':comic.getId().getSerieId()}) }}" title="{{ comic.getTitle()|title }}">
        {% include 'front/search/image.html.twig' %}
      </a>

      <strong>
        <a href="{{ path('serie', {'id':comic.getId().getSerieId()}) }}" title="{{ comic.getTitle()|title }}">{{ comic.getTitle()|title }}</a>
      </strong>

  </div>
{% endfor %}
