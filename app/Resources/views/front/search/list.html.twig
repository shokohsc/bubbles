{% for serie in collection %}
  <div class="comic-item-list">

      <a href="{{ path('serie', {'id':serie.id}) }}" title="{{ serie.title|title }}">
        {% include 'front/comic/image.html.twig' %}
      </a>

      <strong>
        <a href="{{ path('serie', {'id':serie.id}) }}" title="{{ serie.title|title }}">{{ serie.title|title }}</a>
      </strong>

  </div>
{% endfor %}
