{% for comic in collection %}
  <div class="comic-item-list">

      <a href="{{ path('serie', {'id':comic.getId()}) }}" title="{{ comic.getTitle()|title }}">
        {% include 'front/comic/image.html.twig' %}
      </a>

      <strong>
        <a href="{{ path('serie', {'id':comic.getId()}) }}" title="{{ comic.getTitle()|title }}">{{ comic.getTitle()|title }}</a>
      </strong>

  </div>
{% endfor %}
