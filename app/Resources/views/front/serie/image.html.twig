<img
  id="incredible_{{ serie.getId().getSerieId() }}"
  class="img-thumbnail"
  src="{{ serie.getThumbnail().getPath() }}/portrait_incredible.{{ serie.getThumbnail().getExtension() }}"
  alt="{{ serie.getTitle()|title }}"
  height="324"
  width="216"
/>
